const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        primary: "#2b343d",
        secondary: "#1d2528",
        accent: "#f0463b",
      },
    },
  },

  plugins: [],
};

module.exports = config;
